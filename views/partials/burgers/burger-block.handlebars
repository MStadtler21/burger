<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eating time{{else}}Not eating time{{/if}}
	</button>
	<button class="delete-burger" data-id="{{this.id}}">Get Rid of It</button>
</li>